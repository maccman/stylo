@import 'mixins'

.colorPicker {
  -webkit-user-select: none
  overflow: hidden
  hbox()
  padding: 6px
  width: 425px

  canvas {
    display: block
  }

  label {
    display: block
    overflow: hidden

    span {
      display: block
      float: left
      width: 30px
      line-height: 25px

      &:after {
        content: ':'
      }
    }

    input {
      width: 60px
    }
  }

  .spectrum, .gradient, .alpha {
    cursor: pointer
  }

  .preview {
    background: url(grid.png) repeat
  }

  .alpha {
    background: url(grid.png) repeat
    margin: 0 6px 0 0
  }

  .preview {
    margin: 5px 0 10px
  }

  .preview, .preview .inner {
    height: 55px
  }

  .original {
    height: 5px
  }

  button.save {
    float: right
  }
}